<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <h1>
      Vote #<%= @vote.id %> details
    </h1>

    <div class="row mb-3">
      <div class="col">
        <a href="/votes" class="btn btn-block btn-outline-secondary">
          Go back
        </a>
      </div>

      <div class="col">
        <a href="/delete_vote/<%= @vote.id %>" class="btn btn-block btn-outline-secondary">
          Delete vote
        </a>
      </div>
    </div>

    <dl>
      <dt>
        Proposal
      </dt>
      <dd>
        <%= @vote.proposal_id %>
      </dd>

      <dt>
        User
      </dt>
      <dd>
        <%= @vote.user_id %>
      </dd>

      <dt>
        Created at
      </dt>
      <dd>
        <%= time_ago_in_words(@vote.created_at) %> ago
      </dd>

      <dt>
        Updated at
      </dt>
      <dd>
        <%= time_ago_in_words(@vote.updated_at) %> ago
      </dd>
    </dl>
  </div>
</div>

<hr>


<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <h2>
      Edit vote
    </h2>

    <form action="/modify_vote/<%= @vote.id %>"  method="post" >
      <div class="form-group">
        <label for="proposal_id">
          Proposal
        </label>

        <input type="text" id="proposal_id" name="proposal_id" class="form-control" value="<%= @vote.proposal_id %>">
      </div>

      <div class="form-group">
        <label for="user_id">
          User
        </label>

        <input type="text" id="user_id" name="user_id" class="form-control" value="<%= @vote.user_id %>">
      </div>

      <button class="btn btn-block btn-outline-secondary">
        Create vote
      </button>
    </form>
  </div>
</div>

<hr>
